{
  "name": "serverless-lambda-sandbox",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serverless offline start --reloadHandler",
    "dev": "npx serverless dev",
    "deploy": "npx serverless deploy",
    "remove": "npx serverless remove",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.17.7",
    "serverless": "^4.6.2",
    "serverless-offline": "^14.4.0"
  }
}
